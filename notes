/* ===========================
   syntax for darkmoe na ilalagay sa js files
=========================== */

/* ===========================
   document.addEventListener("DOMContentLoaded", function () {
    const savedTheme = localStorage.getItem("theme") || "light";
    if (savedTheme === "dark") {
        document.body.classList.add("dark");
    }
});
=========================== */